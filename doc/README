To build the site:

sphinx-build -a -b html source build

To generate all the function docs automatically, use

sphinx-apidoc -o source/modules/ ../

and change the title in modules.rst

The "pyqz_demo.rst" page must be modified prior to compilation:

Note: should be changed to .. Note:: 

Warning: should be changed to .. Warning::